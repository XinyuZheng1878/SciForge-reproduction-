# @sciforge/workflow

Workflow MCP facade worker for SciForge.

This package exposes agent-facing MCP tools and resources while delegating workflow
execution, approval handling, loop execution, webhook triggers, cron scheduling, and
code nodes to the existing GUI workflow runtime through `/workflow/internal/*`.
