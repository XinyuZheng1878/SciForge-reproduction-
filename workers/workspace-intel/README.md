# @sciforge/workspace-intel

Read-only workspace intelligence worker for SciForge runtimes.

It exposes guarded workspace listing, tree, file read, preview, reference, and skill discovery services through a pure Node service and an MCP stdio server. The worker never writes, renames, deletes, or reads clipboard/image state.
