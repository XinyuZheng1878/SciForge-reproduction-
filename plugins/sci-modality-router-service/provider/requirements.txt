# expert-translator provider (5 GPU transformer experts + ChemLLM delegate).
# Install into a CUDA-enabled Python 3.10+ env (e.g. `conda create -n serve python=3.10`).
# Install a torch build matching your CUDA before the rest:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
transformers>=4.40
accelerate>=0.30
einops>=0.7            # required by Nucleotide-Transformer
sentencepiece>=0.1.99  # tokenizers for several experts
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
requests>=2.31

# The molecule expert delegates to ChemLLM-7B served by vLLM (separate process, GPU 0):
#   pip install vllm>=0.4
# vLLM pins its own torch/CUDA; install it in its own env if it conflicts with the above.
