{
  "name": "sciforge-vision-router-service",
  "version": "0.1.0",
  "description": "Model-Router-managed SciForge vision translator worker: translates visual input into natural-language evidence via a user-configured provider. Translate-only; never reasons.",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "sciforge": {
    "distribution": "private-internal-service",
    "publicNpmPackage": false,
    "commercialBundlePolicy": "May be bundled only as part of the SciForge product under the project owner's commercial terms; not published or relicensed as a public npm package."
  },
  "bin": {
    "sciforge-vision-router": "./src/index.ts"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env --import tsx src/index.ts",
    "dev": "node --env-file-if-exists=.env --watch --import tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
